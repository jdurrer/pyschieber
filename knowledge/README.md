# Project Knowledge

Use this area to preserve context that should survive beyond one coding session.

Each knowledge file starts with frontmatter containing `type`, `status`, and `description`. Add non-trivial decisions, current status, follow-up tasks, or measurements to the matching subdirectory, then update `knowledge/INDEX.md`.

One-line fixes and genuinely trivial changes do not need a knowledge entry.
